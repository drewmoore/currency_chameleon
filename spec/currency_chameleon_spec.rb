require "spec_helper"

describe CurrencyChameleon do
  it "has a version number" do
    expect(CurrencyChameleon::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
